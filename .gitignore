node_modules/
.next/
.env*.local
.DS_Store

# Review-loop scratch — captured diffs, regenerable, not source.
# The ledger and its archive are deliberately NOT ignored: they are the loop's
# recovery artifact, they must survive a fresh clone or worktree, and
# Gemini-based harnesses skip git-ignored paths during file discovery.
.roster/review/

# Subagent-driven development scratch
.superpowers/
